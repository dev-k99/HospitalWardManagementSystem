@model WardSystemProject.Models.MedicalCondition

@{
    ViewData["Title"] = "Create Medical Condition Record";
}

<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-8 col-lg-6">
            <div class="card">
                <div class="card-header bg-info text-white">
                    <h4 class="mb-0">
                        <i class="bi bi-heart-pulse"></i> Create New Medical Condition Record
                    </h4>
                </div>
                <div class="card-body">
                    <form asp-action="CreateMedicalCondition" method="post" class="needs-validation" novalidate>
                        <div asp-validation-summary="ModelOnly" class="alert alert-danger" role="alert"></div>
                        
                        <div class="mb-3">
                            <label asp-for="PatientId" class="form-label fw-bold">
                                <i class="bi bi-person"></i> Patient
                            </label>
                            <select asp-for="PatientId" asp-items="ViewBag.PatientId" class="form-select" required>
                                <option value="">-- Select Patient --</option>
                            </select>
                            <span asp-validation-for="PatientId" class="text-danger"></span>
                            <div class="form-text">Select the patient who has this medical condition</div>
                        </div>

                        <div class="mb-3">
                            <label asp-for="ConditionName" class="form-label fw-bold">
                                <i class="bi bi-heart-pulse"></i> Medical Condition
                            </label>
                            <input asp-for="ConditionName" class="form-control" placeholder="Enter medical condition (e.g., Diabetes, Hypertension)" required />
                            <span asp-validation-for="ConditionName" class="text-danger"></span>
                            <div class="form-text">Enter the name of the medical condition (e.g., Diabetes, Hypertension, Asthma)</div>
                        </div>

                        <div class="d-grid gap-2 d-md-flex justify-content-md-end">
                            <a asp-action="ManageMedicalConditions" class="btn btn-secondary me-md-2">
                                <i class="bi bi-arrow-left"></i> Back to List
                            </a>
                            <button type="submit" class="btn btn-info">
                                <i class="bi bi-check-circle"></i> Create Medical Condition Record
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
    <script>
        // Client-side validation
        (function () {
            'use strict'
            var forms = document.querySelectorAll('.needs-validation')
            Array.prototype.slice.call(forms).forEach(function (form) {
                form.addEventListener('submit', function (event) {
                    if (!form.checkValidity()) {
                        event.preventDefault()
                        event.stopPropagation()
                    }
                    form.classList.add('was-validated')
                }, false)
            })
        })()
    </script>
} 