@model WardSystemProject.Models.Prescription

@{
    ViewData["Title"] = "Edit Prescription";
}

<div class="container mt-4">
    <h2 class="mb-4 text-primary">Edit Prescription</h2>

    <form asp-action="EditPrescription" method="post" class="needs-validation" novalidate>
        @Html.AntiForgeryToken()
        <input type="hidden" asp-for="Id" />
        <div class="form-group">
            <label asp-for="PatientId" class="control-label text-dark fw-bold"></label>
            @Html.DropDownListFor(model => model.PatientId, (SelectList)ViewBag.PatientId, "Select Patient", new { @class = "form-control custom-select", required = "required" })
            <span asp-validation-for="PatientId" class="text-danger"></span>
        </div>
        <div class="form-group">
            <label asp-for="DoctorId" class="control-label text-dark fw-bold"></label>
            @Html.DropDownListFor(model => model.DoctorId, (SelectList)ViewBag.DoctorId, "Select Doctor", new { @class = "form-control custom-select", required = "required" })
            <span asp-validation-for="DoctorId" class="text-danger"></span>
        </div>
        <div class="form-group">
            <label asp-for="MedicationId" class="control-label text-dark fw-bold"></label>
            @Html.DropDownListFor(model => model.MedicationId, (SelectList)ViewBag.MedicationId, "Select Medication", new { @class = "form-control custom-select", required = "required" })
            <span asp-validation-for="MedicationId" class="text-danger"></span>
        </div>
        <div class="form-group">
            <label asp-for="DosageInstructions" class="control-label text-dark fw-bold"></label>
            <textarea asp-for="DosageInstructions" class="form-control" rows="3" required></textarea>
            <span asp-validation-for="DosageInstructions" class="text-danger"></span>
        </div>
        <div class="form-group">
            <label asp-for="PrescriptionDate" class="control-label text-dark fw-bold"></label>
            <input asp-for="PrescriptionDate" class="form-control" type="date" required />
            <span asp-validation-for="PrescriptionDate" class="text-danger"></span>
        </div>
        <div class="form-group">
            <input type="submit" value="Save" class="btn btn-primary" />
            <a asp-action="ManagePrescriptions" class="btn btn-secondary ms-2">Back to List</a>
        </div>
    </form>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
